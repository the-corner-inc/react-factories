export default function Home() {
  return (
    <main className="flex flex-1 items-center justify-center p-6">
      <div className="max-w-2xl rounded-lg border border-zinc-200 p-6 dark:border-zinc-800">
        <h1 className="text-xl font-semibold">react-factories registry shell</h1>
        <p className="mt-2 text-sm text-zinc-600 dark:text-zinc-400">
          This repository is intentionally scaffold-only. Add reusable components,
          sections, layouts, and registry entries as the next step.
        </p>
      </div>
    </main>
  );
}
